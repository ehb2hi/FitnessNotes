import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fitnessnotes.app',
  appName: 'fitness-notes',
  webDir: 'dist/browser'
};

export default config;
